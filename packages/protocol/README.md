# Riftsend Protocol Package

Location: `packages/protocol/`

This package will contain shared protocol definitions used by the web app and, where appropriate, the signaling server.

Planned contents:

- protocol constants
- signaling message definitions
- control message definitions
- manifest definitions
- frame metadata definitions
- validation helpers
